<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="Member"> 
 	<insert id="insertMember" parameterType="member">
 		insert into memberDB(m_id, m_password, m_name, m_email,m_phone) 
 			values(#{m_id},#{m_password},#{m_name},#{m_email},#{m_phone})
 	</insert>
 	
 	<select id="loginMember" parameterType="member" resultType="String">
 		select m_id from memberDB where m_id= #{m_id} and m_password= #{m_password};
 	</select>
 	
 </mapper>